<UserControl x:Class="DshLauncher.Views.QuickRefView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
        <StackPanel>
            <TextBlock Style="{StaticResource PageHeader}" Text="速查"/>
            <TextBlock Style="{StaticResource PageSubtitle}" Text="常用 DeepSeek Harness 命令 + Prompt 模板，点「复制」即复制到剪贴板。"/>

            <StackPanel Orientation="Horizontal" Margin="0,8,0,0">
                <TextBlock Text="{Binding Status}" Foreground="{DynamicResource Brush.Accent.Blue}" FontSize="12" TextWrapping="Wrap"/>
            </StackPanel>

            <!-- 常用命令 -->
            <Border Style="{StaticResource Card}" Margin="0,18,0,0">
                <StackPanel>
                    <TextBlock Text="{Binding Commands.Title}" FontSize="16" FontWeight="SemiBold"/>
                    <ItemsControl ItemsSource="{Binding Commands.Items}" Margin="0,12,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Brush.Surface}" CornerRadius="8" Padding="12,10" Margin="0,0,0,8"
                                        BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                            <TextBlock Text="{Binding Content}" FontFamily="Consolas, Microsoft YaHei UI"
                                                       FontSize="12" TextWrapping="Wrap" Margin="0,4,0,0"
                                                       Foreground="{DynamicResource Brush.Text.Primary}"/>
                                            <TextBlock Text="{Binding Desc}" Foreground="{DynamicResource Brush.Text.Secondary}"
                                                       FontSize="11" Margin="0,3,0,0"/>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="复制" Padding="12,5" Margin="10,0,0,0"
                                                VerticalAlignment="Top"
                                                Command="{Binding DataContext.CopyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>

            <!-- Prompt 模板 -->
            <Border Style="{StaticResource Card}" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="{Binding Prompts.Title}" FontSize="16" FontWeight="SemiBold"/>
                    <ItemsControl ItemsSource="{Binding Prompts.Items}" Margin="0,12,0,0">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource Brush.Surface}" CornerRadius="8" Padding="12,10" Margin="0,0,0,8"
                                        BorderBrush="{DynamicResource Brush.Border}" BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <StackPanel>
                                            <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="13"/>
                                            <TextBlock Text="{Binding Content}" FontSize="12" TextWrapping="Wrap"
                                                       Margin="0,4,0,0" Foreground="{DynamicResource Brush.Text.Primary}"/>
                                            <TextBlock Text="{Binding Desc}" Foreground="{DynamicResource Brush.Text.Secondary}"
                                                       FontSize="11" Margin="0,3,0,0"/>
                                        </StackPanel>
                                        <Button Grid.Column="1" Content="复制" Padding="12,5" Margin="10,0,0,0"
                                                VerticalAlignment="Top"
                                                Command="{Binding DataContext.CopyCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</UserControl>
