<UserControl x:Class="DshLauncher.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- 日志页 · 实时 stdout/stderr（PRD §5.1 / §6：数组缓冲 10 万行不卡） -->
    <DockPanel>
        <DockPanel DockPanel.Dock="Top" Margin="0,0,0,14">
            <TextBlock Style="{StaticResource PageHeader}" Text="日志" DockPanel.Dock="Left" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <TextBlock Text="{Binding LineCount}" Foreground="{DynamicResource Brush.Text.Secondary}"
                           VerticalAlignment="Center" Margin="0,0,16,0"/>
                <Button Content="{Binding PauseButtonText}" Command="{Binding TogglePauseCommand}"/>
                <Button Content="复制" Command="{Binding CopyAllCommand}" Margin="10,0,0,0"/>
                <Button Content="清空" Command="{Binding ClearCommand}" Margin="10,0,0,0"/>
            </StackPanel>
        </DockPanel>

        <Border Style="{StaticResource Card}" Padding="0">
            <ListBox x:Name="LogList"
                     ItemsSource="{Binding Lines}"
                     Background="{DynamicResource Brush.Surface}" BorderThickness="0"
                     FontFamily="Consolas, Microsoft YaHei UI" FontSize="12.5"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     VirtualizingPanel.IsVirtualizing="True"
                     VirtualizingPanel.VirtualizationMode="Recycling">
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        <Setter Property="Focusable" Value="False"/>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding Timestamp, StringFormat=HH:mm:ss.fff}"
                                       Foreground="{DynamicResource Brush.Text.Dim}" Width="92"/>
                            <TextBlock Text="[" Foreground="{DynamicResource Brush.Text.Dim}"/>
                            <TextBlock Text="{Binding Source}" Width="42">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Foreground" Value="{DynamicResource Brush.Accent.Orange}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Source}" Value="Stdout">
                                                <Setter Property="Foreground" Value="{DynamicResource Brush.Text.Secondary}"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <TextBlock Text="] " Foreground="{DynamicResource Brush.Text.Dim}"/>
                            <TextBlock Text="{Binding Text}" Foreground="{DynamicResource Brush.Text.Primary}"/>
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>
    </DockPanel>
</UserControl>
